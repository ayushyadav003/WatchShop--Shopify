
{% if section.settings.show_topbar %}
      <div class="announcement-container">
        <div class="announcement-bar" {{ block.shopify_attributes }}>
          	<div class="slide">{{ block.settings.announcement_text }}</div>
        </div>
       <div id='dismiss' class="cross-announcement-bar" onclick="dismiss(this)">&#215</div>
      </div>
{% endif %}

  <div class="announcement-container">
    Hello
</div>


{% schema %}
{
  "name": "Announcement",
  "class": "announcement-container",
  "max_blocks": 5,
  "settings": [  
	{
          "type": "checkbox",
          "id": "show_topbar",
          "label": "Show bar",
          "default": true
      }
],
  "blocks": [
    {
      "name": "Announcement",
      "type": "Text",
      "settings": [
          {
            "type": "text",
            "id": "announcement_text",
            "default": "Welcome to our store",
            "label": "Text"
          }
        ]
      }
	]
}
{% endschema %}


{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
